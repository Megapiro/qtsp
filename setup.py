from setuptools import find_packages, setup

setup(
    name='qtsp',
    packages=find_packages(),
    version='0.0.0',
    description='',
    author='',
    license='',
    install_requires=['numpy', 'pandas', 'dimod', 'dwave-neal', 'dwave-hybrid', 'pyqubo'],
    setup_requires=[],
)